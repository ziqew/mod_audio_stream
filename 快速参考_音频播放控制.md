# 音频播放控制 - 快速参考

本文档提供 mod_audio_stream 音频播放控制的快速参考。

完整文档请查看：[音频播放控制指南.md](./音频播放控制指南.md)

---

## 快速开始

### 1. 服务器发送音频格式

```json
{
  "type": "streamAudio",
  "data": {
    "audioDataType": "raw",
    "sampleRate": 8000,
    "audioData": "base64编码的音频数据"
  }
}
```

**支持的音频类型**: `raw`, `wav`, `mp3`, `ogg`, `pcmu`, `pcma`

### 2. 监听播放事件

#### Python ESL

```python
import ESL
import json

con = ESL.ESLconnection("localhost", "8021", "ClueCon")
con.events("plain", "CUSTOM mod_audio_stream::play")

while True:
    e = con.recvEvent()
    if e and e.getHeader("Event-Subclass") == "mod_audio_stream::play":
        data = json.loads(e.getBody())
        uuid = e.getHeader("Unique-ID")
        file_path = data.get("file")
        
        # 播放音频
        con.api(f"uuid_broadcast {uuid} {file_path} both")
```

#### Lua 脚本

```lua
local con = freeswitch.EventConsumer("CUSTOM", "mod_audio_stream::play")
local uuid = session:getVariable("uuid")

local event = con:pop(1)
if event then
    local cjson = require("cjson")
    local data = cjson.decode(event:getBody())
    
    if data.file then
        session:streamFile(data.file)
    end
end
```

### 3. 播放音频

#### 方法 1: uuid_broadcast（推荐）

```bash
uuid_broadcast <uuid> <file_path> both
```

```python
con.api(f"uuid_broadcast {uuid} {file_path} both")
```

#### 方法 2: uuid_displace（混音播放）

```bash
uuid_displace <uuid> start <file_path> 0 m
```

```python
con.api(f"uuid_displace {uuid} start {file_path} 0 m")
```

### 4. 停止播放

#### 停止 broadcast

```bash
uuid_broadcast <uuid> stop
```

```python
con.api(f"uuid_broadcast {uuid} stop")
```

#### 停止 displace

```bash
uuid_displace <uuid> stop <file_path>
```

```python
con.api(f"uuid_displace {uuid} stop {file_path}")
```

---

## 常用命令对照表

| 操作 | fs_cli 命令 | Python ESL |
|------|-------------|------------|
| 播放音频 | `uuid_broadcast <uuid> <file> both` | `con.api(f"uuid_broadcast {uuid} {file} both")` |
| 停止播放 | `uuid_broadcast <uuid> stop` | `con.api(f"uuid_broadcast {uuid} stop")` |
| 混音播放 | `uuid_displace <uuid> start <file> 0 m` | `con.api(f"uuid_displace {uuid} start {file} 0 m")` |
| 停止混音 | `uuid_displace <uuid> stop <file>` | `con.api(f"uuid_displace {uuid} stop {file}")` |
| 清除队列 | `uuid_break <uuid> all` | `con.api(f"uuid_break {uuid} all")` |
| 暂停流 | `uuid_audio_stream <uuid> pause` | `con.api(f"uuid_audio_stream {uuid} pause")` |
| 恢复流 | `uuid_audio_stream <uuid> resume` | `con.api(f"uuid_audio_stream {uuid} resume")` |

---

## 事件格式

### mod_audio_stream::play 事件

```json
{
  "audioDataType": "raw",
  "sampleRate": 8000,
  "file": "/tmp/freeswitch/audio_12345678.r8"
}
```

**事件头信息**:
- `Event-Name`: CUSTOM
- `Event-Subclass`: mod_audio_stream::play
- `Unique-ID`: 通道 UUID

---

## 完整流程图

```
WebSocket 服务器
    ↓
发送 streamAudio 消息
    ↓
mod_audio_stream 接收并处理
    ↓
解码 base64 音频数据
    ↓
保存到临时文件
    ↓
触发 mod_audio_stream::play 事件
    ↓
ESL/Lua 脚本监听事件
    ↓
获取文件路径
    ↓
调用 uuid_broadcast/uuid_displace
    ↓
FreeSWITCH 播放音频给呼叫者
```

---

## 最小示例

### WebSocket 服务器（Node.js）

```javascript
const WebSocket = require('ws');
const fs = require('fs');

const wss = new WebSocket.Server({ port: 8080 });

wss.on('connection', (ws) => {
    // 读取音频文件
    const audioData = fs.readFileSync('./audio.wav');
    
    // 发送音频
    const response = {
        type: 'streamAudio',
        data: {
            audioDataType: 'wav',
            audioData: audioData.toString('base64')
        }
    };
    
    ws.send(JSON.stringify(response));
});
```

### 播放控制（Python）

```python
import ESL
import json

con = ESL.ESLconnection("localhost", "8021", "ClueCon")
con.events("plain", "CUSTOM mod_audio_stream::play")

while True:
    e = con.recvEvent()
    if e:
        data = json.loads(e.getBody())
        uuid = e.getHeader("Unique-ID")
        file_path = data.get("file")
        
        if file_path:
            con.api(f"uuid_broadcast {uuid} {file_path} both")
```

---

## 常见问题速查

| 问题 | 解决方案 |
|------|---------|
| 音频没有播放 | 检查文件路径是否存在，通道是否活动 |
| 播放被中断 | 使用 `uuid_displace` 而不是 `uuid_broadcast` |
| 音频质量差 | 检查采样率是否正确匹配 |
| 临时文件未清理 | 会话结束时自动清理，或手动清理 `/tmp/freeswitch/audio_*` |
| 多个音频播放 | 实现队列管理或在服务器端合并音频 |

---

## 下一步

- 阅读完整文档：[音频播放控制指南.md](./音频播放控制指南.md)
- 查看集成示例：[FreeSWITCH集成指南.md](./FreeSWITCH集成指南.md)
- 学习 ESL 使用：[Python_ESL_使用指南.md](./Python_ESL_使用指南.md)
- 了解内部原理：[代码逻辑分析.md](./代码逻辑分析.md)

---

**提示**: 这是快速参考文档。更详细的说明、完整示例和故障排除，请查看 [音频播放控制指南.md](./音频播放控制指南.md)
