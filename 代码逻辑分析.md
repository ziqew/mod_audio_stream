# mod_audio_stream 代码仓库逻辑分析

## 项目概述

**mod_audio_stream** 是一个用于 FreeSWITCH 的生产级 WebSocket 音频流模块，可实现通话音频与外部系统之间的实时双向流传输，具备完整的生命周期管理、线程安全性和可预测的内存使用特性。

### 主要用途

1. **自动语音识别(ASR)** - 将通话音频流式传输到云端 ASR 服务
2. **实时音频处理** - 连接通话到 AI/ML 处理管道进行分析
3. **通话录音/转录** - 流式传输到转录服务
4. **交互式语音应用** - 动态发送和接收音频

### 核心特性

- ✅ 全双工音频流传输（呼叫者 ↔ WebSocket）
- ✅ 支持 base64 编码和原始二进制音频
- ✅ 可动态跟踪、暂停和恢复播放
- ✅ 线程安全，适用于高并发环境（5000+ 并发呼叫）
- ✅ 正确的 FreeSWITCH 会话生命周期管理
- ✅ 有界且可预测的内存使用

---

## 架构设计

### 组件层次结构

```
FreeSWITCH 核心
    ↓
mod_audio_stream.c (模块接口)
    ↓
capture_callback (媒体钩子回调)
    ↓
audio_streamer_glue.cpp (C++/C 桥接层)
    ↓
AudioStreamer 类 (业务逻辑)
    ↓
libwsc (WebSocket 客户端 - RFC-6455 标准，基于 libevent)
    ↓
OpenSSL/TLS (安全层)
```

### 核心组件说明

| 组件 | 作用 |
|------|------|
| **mod_audio_stream.c** | FreeSWITCH 模块入口点，API 命令解析，媒体钩子设置 |
| **audio_streamer_glue.cpp** | C++/C 桥接，音频帧处理，重采样，WebSocket 管理 |
| **AudioStreamer** | 核心类，管理 WebSocket 生命周期和消息路由 |
| **libwsc** | 轻量级 WebSocket 客户端（RFC-6455 标准，基于 libevent） |
| **speex_resampler** | 音频重采样引擎（Speex DSP 库） |
| **SwitchBuffer** | 循环缓冲区，用于聚合音频帧为可变大小的数据包 |

---

## 音频流传输流程详解

### 阶段一：会话初始化

**API 命令格式：**
```
uuid_audio_stream <uuid> start <ws-url> <mix-type> <sampling-rate> [metadata]
```

**参数说明：**
- `uuid`: FreeSWITCH 通道唯一标识符
- `ws-url`: WebSocket URL (`ws://` 或 `wss://`)
- `mix-type`: 音频混合类型
  - `"mono"` - 单声道，仅包含呼叫者音频
  - `"mixed"` - 单声道，包含呼叫者和被叫者混合音频
  - `"stereo"` - 双声道，一个声道为呼叫者，另一个为被叫者
- `sampling-rate`: 采样率
  - `"8k"` = 8000 Hz
  - `"16k"` = 16000 Hz
- `metadata`: 可选的初始元数据（UTF-8 文本）

**初始化步骤：**

1. **验证输入参数**
   - 验证 WebSocket URI 格式
   - 验证采样率（必须是 8000 的倍数）
   - 验证混合类型
   - 验证元数据为有效 UTF-8

2. **创建会话私有数据结构** (`private_t`)
   ```c
   struct private_data {
       switch_mutex_t *mutex;              // 线程同步互斥锁
       char sessionId[256];                // 会话 UUID
       SpeexResamplerState *resampler;     // 音频重采样器（或 NULL）
       responseHandler_t responseHandler;  // 事件回调函数
       void *pAudioStreamer;               // → std::shared_ptr<AudioStreamer>
       char ws_uri[4096];                  // WebSocket URL
       int sampling;                       // 目标采样率
       int channels;                       // 1（单声道）或 2（立体声）
       int audio_paused;                   // 暂停标志
       int close_requested;                // 显式关闭标志
       int cleanup_started;                // 清理进行中标志
       char initialMetadata[8192];         // 初始元数据
       switch_buffer_t *sbuffer;           // 帧聚合缓冲区
       int rtp_packets;                    // 聚合因子（20ms × 因子）
   };
   ```

3. **初始化 Speex 重采样器**
   - 如果源采样率不等于目标采样率，创建重采样器
   - 配置输入/输出采样率和声道数
   - 使用 Speex DSP 库进行高质量重采样

4. **创建音频帧聚合缓冲区**
   ```c
   buffer_len = FRAME_SIZE_8000 * (sampling/8000) * channels * rtp_packets
   switch_buffer_create_dynamic(&sbuffer, buffer_len, buffer_len * 2, 0)
   ```
   - `rtp_packets` 由 `STREAM_BUFFER_SIZE` 通道变量控制
   - 默认值为 1（20ms 数据包）
   - 可设置为更高值以发送更大的聚合数据包

5. **创建 WebSocket 客户端并发起连接**
   ```cpp
   auto streamer = AudioStreamer::create(
       uuid, wsUri, callback, deflate, heart_beat,
       suppressLog, extra_headers, tls_cafile, tls_keyfile,
       tls_certfile, tls_disable_hostname_validation
   );
   ```

6. **将媒体钩子附加到 FreeSWITCH 通道**
   ```c
   switch_core_media_bug_add(session, MY_BUG_NAME, NULL, 
                             capture_callback, pUserData, 0, flags, &bug)
   ```

### 阶段二：音频捕获与帧处理

**主处理循环：**
```
FreeSWITCH 音频流
    ↓ (每 20ms)
capture_callback(SWITCH_ABC_TYPE_READ)
    ↓
stream_frame(bug) ← 主音频处理循环
    ↓
从媒体钩子读取音频帧
    ↓
重采样（如需要）
    ↓
帧聚合（缓冲区）
    ↓
二进制编码（L16 PCM）
    ↓
WebSocket 传输
```

**详细的帧处理逻辑：**

1. **无需重采样的情况**（源采样率 == 目标采样率）：
   ```c
   while (switch_core_media_bug_read(bug, &frame, SWITCH_TRUE)) {
       // frame.data = 原始音频字节（L16 16位 PCM）
       // frame.datalen = 字节数
       // frame.samples = 采样点数
       
       switch_buffer_write(tech_pvt->sbuffer, frame.data, frame.datalen);
       
       // 检查缓冲区是否已满
       if (switch_buffer_inuse(tech_pvt->sbuffer) >= buffer_len) {
           // 读取聚合数据并发送
           switch_buffer_read(tech_pvt->sbuffer, data, buffer_len);
           pAudioStreamer->writeBinary(data, buffer_len);
       }
   }
   ```

2. **需要重采样的情况**（源采样率 ≠ 目标采样率）：
   ```c
   while (switch_core_media_bug_read(bug, &frame, SWITCH_TRUE)) {
       spx_uint32_t in_len = frame.samples / tech_pvt->channels;
       spx_uint32_t out_len = out_buf_size / sizeof(int16_t) / tech_pvt->channels;
       
       // 执行重采样
       speex_resampler_process_interleaved_int(
           tech_pvt->resampler,
           (const spx_int16_t*)frame.data,
           &in_len,
           (spx_int16_t*)out,
           &out_len
       );
       
       // 将重采样后的数据写入缓冲区
       int16_t* data_ptr = (int16_t*)out;
       size_t data_len = out_len * tech_pvt->channels * sizeof(int16_t);
       switch_buffer_write(tech_pvt->sbuffer, data_ptr, data_len);
       
       // 发送聚合数据
       if (switch_buffer_inuse(tech_pvt->sbuffer) >= buffer_len) {
           switch_buffer_read(tech_pvt->sbuffer, data, buffer_len);
           pAudioStreamer->writeBinary(data, buffer_len);
       }
   }
   ```

**音频格式：**
- **编码格式**: L16（16位有符号 PCM，大端序）
- **采样率**: 可配置（8000 Hz, 16000 Hz 等）
- **声道**: 1（单声道）或 2（立体声）
- **帧大小**: 默认 20ms，可通过 `STREAM_BUFFER_SIZE` 配置

### 阶段三：WebSocket 连接管理

**连接生命周期：**

```
AudioStreamer::create()
    ↓
client.connect() (异步)
    ↓
onOpen() 回调触发
    ↓
发送初始元数据（如果提供）
    ↓
触发 mod_audio_stream::connect 事件
    ↓
流传输激活（发送二进制帧）
    ↓
服务器响应（JSON 消息）
    ↓
eventCallback(MESSAGE, ...)
    ↓
处理音频播放（如果 type="streamAudio"）
    ↓
触发 mod_audio_stream::json 或 mod_audio_stream::play 事件
```

**回调系统：**

```cpp
// 设置 WebSocket 回调
client.setMessageCallback([weak_self](const std::string& message) {
    auto self = weak_self.lock();
    if (!self || self->isCleanedUp()) return;
    self->eventCallback(MESSAGE, message.c_str());
});

client.setOpenCallback([weak_self]() {
    auto self = weak_self.lock();
    if (!self || self->isCleanedUp()) return;
    // 构建连接成功事件
    cJSON* root = cJSON_CreateObject();
    cJSON_AddStringToObject(root, "status", "connected");
    char* json_str = cJSON_PrintUnformatted(root);
    self->eventCallback(CONNECT_SUCCESS, json_str);
    cJSON_Delete(root);
    switch_safe_free(json_str);
});

client.setErrorCallback([weak_self](int code, const std::string& msg) {
    // 处理连接错误，触发 CONNECT_ERROR 事件
    // 关闭媒体钩子
});

client.setCloseCallback([weak_self](int code, const std::string& reason) {
    // 处理连接关闭，触发 CONNECTION_DROPPED 事件
});
```

### 阶段四：音频播放（双向音频）

**服务器发送音频播放数据：**

```json
{
  "type": "streamAudio",
  "data": {
    "audioDataType": "raw",  // 或 wav, mp3, ogg, pcmu, pcma
    "sampleRate": 8000,
    "audioData": "<base64 编码的音频数据>"
  }
}
```

**处理流程：**

1. **解析 JSON 响应**
   ```cpp
   ProcessResult processMessage(const std::string& msg) {
       cJSON* root = cJSON_Parse(msg.c_str());
       cJSON* type = cJSON_GetObjectItem(root, "type");
       
       if (type && strcmp(type->valuestring, "streamAudio") == 0) {
           cJSON* data = cJSON_GetObjectItem(root, "data");
           cJSON* audioDataType = cJSON_GetObjectItem(data, "audioDataType");
           cJSON* audioData = cJSON_GetObjectItem(data, "audioData");
           cJSON* sampleRate = cJSON_GetObjectItem(data, "sampleRate");
           
           // 解码 base64 音频数据
           std::string decoded = base64_decode(audioData->valuestring);
           
           // 写入临时文件
           std::string file_path = write_temp_file(decoded, audioDataType, sampleRate);
           
           // 跟踪文件以便清理
           m_Files.insert(file_path);
           
           // 构建包含文件路径的响应
           cJSON* result = cJSON_CreateObject();
           cJSON_AddStringToObject(result, "audioDataType", audioDataType->valuestring);
           cJSON_AddNumberToObject(result, "sampleRate", sampleRate->valueint);
           cJSON_AddStringToObject(result, "file", file_path.c_str());
           
           return ProcessResult{SWITCH_TRUE, cJSON_PrintUnformatted(result)};
       }
   }
   ```

2. **支持的音频格式**
   - `raw` - 原始 PCM 数据（.r8 文件）
   - `wav` - WAV 格式（.wav 文件）
   - `mp3` - MP3 格式（.mp3 文件）
   - `ogg` - OGG 格式（.ogg 文件）
   - `pcmu` / `pcma` - μ-law / A-law 编码

3. **触发播放事件**
   ```c
   // 触发 mod_audio_stream::play 事件
   m_notify(session, EVENT_PLAY, json_with_file_path);
   ```

4. **FreeSWITCH 播放音频**
   - 应用程序可以监听 `mod_audio_stream::play` 事件
   - 使用事件中的 `file` 路径调用 FreeSWITCH 播放应用
   - 音频将播放给呼叫者

### 阶段五：会话清理

**停止流传输：**
```
API 命令: uuid_audio_stream <uuid> stop [final_text]
```

**清理流程：**

```c
switch_status_t stream_session_cleanup(switch_core_session_t *session, 
                                      char* text, 
                                      int channelIsClosing) {
    // 1. 获取会话私有数据
    private_t* tech_pvt = get_tech_pvt(session);
    if (!tech_pvt) return SWITCH_STATUS_FALSE;
    
    // 2. 防止重入
    if (tech_pvt->cleanup_started) return SWITCH_STATUS_SUCCESS;
    tech_pvt->cleanup_started = 1;
    
    // 3. 获取 AudioStreamer（线程安全）
    switch_mutex_lock(tech_pvt->mutex);
    std::shared_ptr<AudioStreamer> streamer = get_streamer(tech_pvt);
    switch_mutex_unlock(tech_pvt->mutex);
    
    // 4. 发送最终文本消息（如果提供）
    if (streamer && text && strlen(text) > 0) {
        streamer->writeText(text);
    }
    
    // 5. 删除临时音频文件
    if (streamer) {
        streamer->deleteFiles();
    }
    
    // 6. 标记已清理并断开连接
    if (streamer) {
        streamer->markCleanedUp();  // 禁用回调
        streamer->disconnect();     // 关闭 WebSocket
    }
    
    // 7. 销毁技术私有数据
    destroy_tech_pvt(tech_pvt);
    
    // 8. 移除媒体钩子（仅当通道未关闭时）
    if (!channelIsClosing) {
        switch_channel_t *channel = switch_core_session_get_channel(session);
        switch_media_bug_t *bug = switch_channel_get_private(channel, MY_BUG_NAME);
        if (bug) {
            switch_core_media_bug_remove(session, &bug);
            switch_channel_set_private(channel, MY_BUG_NAME, NULL);
        }
    }
    
    return SWITCH_STATUS_SUCCESS;
}
```

**资源清理详情：**

1. **WebSocket 连接**
   - 调用 `client.disconnect()` 关闭连接
   - 清空所有回调函数防止竞态条件

2. **临时文件**
   - 删除所有播放功能创建的临时音频文件
   - 文件路径存储在 `m_Files` 集合中

3. **重采样器**
   ```c
   if (tech_pvt->resampler) {
       speex_resampler_destroy(tech_pvt->resampler);
       tech_pvt->resampler = NULL;
   }
   ```

4. **缓冲区**
   ```c
   if (tech_pvt->sbuffer) {
       switch_buffer_destroy(&tech_pvt->sbuffer);
   }
   ```

5. **互斥锁**
   ```c
   if (tech_pvt->mutex) {
       switch_mutex_destroy(tech_pvt->mutex);
   }
   ```

---

## API 命令详解

### 1. 启动音频流

```
uuid_audio_stream <uuid> start <wss-url> <mix-type> <sampling-rate> <metadata>
```

**功能**: 附加媒体钩子并开始将音频（L16 格式）流式传输到 WebSocket 服务器。

**示例**:
```
uuid_audio_stream 123e4567-e89b-12d3-a456-426614174000 start ws://example.com/stream mono 8k
uuid_audio_stream 123e4567-e89b-12d3-a456-426614174000 start wss://secure.example.com/stream stereo 16k {"session":"abc123"}
```

### 2. 发送文本消息

```
uuid_audio_stream <uuid> send_text <metadata>
```

**功能**: 向 WebSocket 服务器发送文本消息。需要有效的 UTF-8 文本。

**示例**:
```
uuid_audio_stream 123e4567-e89b-12d3-a456-426614174000 send_text {"command":"start_recognition"}
```

### 3. 停止音频流

```
uuid_audio_stream <uuid> stop <metadata>
```

**功能**: 停止音频流并关闭 WebSocket 连接。如果提供元数据，将在关闭连接前发送。

**示例**:
```
uuid_audio_stream 123e4567-e89b-12d3-a456-426614174000 stop
uuid_audio_stream 123e4567-e89b-12d3-a456-426614174000 stop {"reason":"call_ended"}
```

### 4. 暂停音频流

```
uuid_audio_stream <uuid> pause
```

**功能**: 暂停音频流（WebSocket 连接保持打开）。

### 5. 恢复音频流

```
uuid_audio_stream <uuid> resume
```

**功能**: 恢复音频流。

---

## 通道变量配置

通道变量用于微调 WebSocket 连接和模块日志记录：

| 变量名 | 描述 | 默认值 |
|--------|------|--------|
| `STREAM_MESSAGE_DEFLATE` | `true` 或 `1` 禁用 per-message deflate 压缩 | 关闭（启用压缩） |
| `STREAM_HEART_BEAT` | 秒数，发送心跳的间隔 | 关闭 |
| `STREAM_SUPPRESS_LOG` | `true` 或 `1` 抑制打印到日志 | 关闭 |
| `STREAM_BUFFER_SIZE` | 缓冲区持续时间（毫秒），必须能被 20 整除 | 20 |
| `STREAM_EXTRA_HEADERS` | 附加头的 JSON 对象（字符串格式） | 无 |
| `STREAM_TLS_CA_FILE` | CA 证书或证书包，或特殊值 SYSTEM 或 NONE | SYSTEM |
| `STREAM_TLS_KEY_FILE` | WSS 连接的可选客户端密钥 | 无 |
| `STREAM_TLS_CERT_FILE` | WSS 连接的可选客户端证书 | 无 |
| `STREAM_TLS_DISABLE_HOSTNAME_VALIDATION` | `true` 或 `1` 在 WSS 连接中禁用主机名检查 | false |

**示例配置**:
```xml
<action application="set" data="STREAM_BUFFER_SIZE=100"/>
<action application="set" data="STREAM_HEART_BEAT=30"/>
<action application="set" data="STREAM_EXTRA_HEADERS={'Authorization':'Bearer token123'}"/>
<action application="set" data="STREAM_TLS_CA_FILE=/path/to/ca-bundle.crt"/>
```

---

## 事件系统

模块生成以下自定义事件类型：

### 1. mod_audio_stream::connect

**触发时机**: 成功连接到 WebSocket 服务器

**事件体**:
```json
{
  "status": "connected"
}
```

### 2. mod_audio_stream::disconnect

**触发时机**: 从 WebSocket 服务器断开连接

**事件体**:
```json
{
  "status": "disconnected",
  "message": {
    "code": 1000,
    "reason": "Normal closure"
  }
}
```

**关闭代码说明**:
- `1000` - 正常关闭
- `1001` - 端点离开
- `1006` - 异常关闭
- 其他 WebSocket 标准关闭代码

### 3. mod_audio_stream::error

**触发时机**: 连接出现错误

**事件体**:
```json
{
  "status": "error",
  "message": {
    "code": 1,
    "error": "String explaining the error"
  }
}
```

**错误代码说明**:

| 代码 | 枚举名称 | 含义 |
|:----:|:---------|:-----|
| 1 | `IO` | 读写套接字时的 I/O 错误 |
| 2 | `INVALID_HEADER` | 服务器发送了格式错误的 WebSocket 头 |
| 3 | `SERVER_MASKED` | 服务器帧被掩码（规范不允许） |
| 4 | `NOT_SUPPORTED` | 请求的功能（如扩展）不支持 |
| 5 | `PING_TIMEOUT` | 超时内未收到 PONG |
| 6 | `CONNECT_FAILED` | TCP 连接或 DNS 查询失败 |
| 7 | `TLS_INIT_FAILED` | 无法初始化 SSL/TLS 上下文 |
| 8 | `SSL_HANDSHAKE_FAILED` | 与服务器的 SSL/TLS 握手失败 |
| 9 | `SSL_ERROR` | 通用 OpenSSL 错误（证书、密码等） |
| 10 | `TIMEOUT` | 超时 |
| 11 | `PROTOCOL` | WebSocket 协议错误 |

### 4. mod_audio_stream::json

**触发时机**: 从 WebSocket 端点接收到消息（非 streamAudio 类型）

**事件体**: WebSocket 服务器的原始响应

### 5. mod_audio_stream::play

**触发时机**: 服务器返回音频播放数据

**服务器响应格式**:
```json
{
  "type": "streamAudio",
  "data": {
    "audioDataType": "raw",
    "sampleRate": 8000,
    "audioData": "base64 encoded audio"
  }
}
```

**生成的事件体**:
```json
{
  "audioDataType": "raw",
  "sampleRate": 8000,
  "file": "/tmp/freeswitch/audio_xxxxx.r8"
}
```

**注意**: 原始响应中的 base64 编码音频被替换为文件路径，因为编码数据可能非常大。

---

## 并发安全性与线程模型

### 线程模型

1. **FreeSWITCH 音频线程**
   - 每 20ms 调用一次 `stream_frame()`（通过媒体钩子）
   - 读取、处理和发送音频帧
   - 使用 `switch_mutex_trylock()` 非阻塞锁定

2. **libwsc 事件线程**
   - 异步处理 WebSocket 事件
   - 执行回调（onOpen, onMessage, onError, onClose）
   - 使用 libevent 事件循环

3. **FreeSWITCH 主线程**
   - 处理 API 命令
   - 执行会话初始化和清理

### 同步机制

1. **互斥锁保护**
   ```c
   // 保护对 pAudioStreamer 的访问
   switch_mutex_lock(tech_pvt->mutex);
   std::shared_ptr<AudioStreamer> streamer = get_streamer(tech_pvt);
   switch_mutex_unlock(tech_pvt->mutex);
   ```

2. **智能指针引用计数**
   ```cpp
   // 使用 std::shared_ptr 进行线程安全的引用计数
   std::shared_ptr<AudioStreamer> sp(new AudioStreamer(...));
   
   // WebSocket 回调使用 weak_ptr 安全检查
   void bindCallbacks(std::weak_ptr<AudioStreamer> wp) {
       client.setMessageCallback([wp](const std::string& message) {
           auto self = wp.lock();
           if (!self) return;  // 对象已销毁
           if (self->isCleanedUp()) return;  // 正在清理
           self->eventCallback(MESSAGE, message.c_str());
       });
   }
   ```

3. **原子标志**
   ```cpp
   std::atomic<bool> m_cleanedUp;  // 原子清理状态标志
   
   bool isCleanedUp() const {
       return m_cleanedUp.load(std::memory_order_acquire);
   }
   
   void markCleanedUp() {
       m_cleanedUp.store(true, std::memory_order_release);
   }
   ```

4. **无锁消息路径**
   - WebSocket 回调在关闭期间设置为空函数
   - `isCleanedUp()` 标志防止关闭期间的回调

### 竞态条件预防

1. **会话清理**
   ```c
   // 使用 cleanup_started 标志防止重入
   if (tech_pvt->cleanup_started) return SWITCH_STATUS_SUCCESS;
   tech_pvt->cleanup_started = 1;
   ```

2. **媒体钩子关闭**
   ```c
   // 设置标志防止进一步处理
   tech_pvt->close_requested = 1;
   
   // 在 capture_callback 中检查
   if (tech_pvt->close_requested) {
       return SWITCH_FALSE;
   }
   ```

3. **回调禁用**
   ```cpp
   // 在销毁前清除所有回调
   client.setMessageCallback({});
   client.setOpenCallback({});
   client.setErrorCallback({});
   client.setCloseCallback({});
   ```

---

## 内存管理

### 分配策略

1. **会话分配**
   ```c
   // 使用 FreeSWITCH 会话池分配
   tech_pvt = (private_t*)switch_core_session_alloc(session, sizeof(private_t));
   // 会话销毁时自动释放
   ```

2. **WebSocket 对象**
   ```cpp
   // 使用智能指针自动管理
   std::shared_ptr<AudioStreamer> streamer = AudioStreamer::create(...);
   // 引用计数归零时自动销毁
   ```

3. **重采样器**
   ```c
   // 手动创建和销毁
   tech_pvt->resampler = speex_resampler_init(...);
   // 在清理时销毁
   speex_resampler_destroy(tech_pvt->resampler);
   ```

4. **缓冲区**
   ```c
   // 由 FreeSWITCH 池管理
   switch_buffer_create_dynamic(&tech_pvt->sbuffer, ...);
   switch_buffer_destroy(&tech_pvt->sbuffer);
   ```

5. **临时文件**
   ```cpp
   // 跟踪在集合中，清理时删除
   std::unordered_set<std::string> m_Files;
   
   void deleteFiles() {
       for (const auto& fn : m_Files) {
           ::remove(fn.c_str());
       }
       m_Files.clear();
   }
   ```

### 内存边界

- **缓冲区大小**: 由 `STREAM_BUFFER_SIZE` 控制，确保可预测的内存使用
- **临时文件**: 会话结束时自动清理
- **WebSocket 缓冲区**: 由 libwsc 管理，有限且可预测

---

## 会话生命周期状态机

```
[初始化]
  ↓ stream_session_init()
  ├─→ [初始化 WebSocket]
  │     ↓
  │     client.connect() 异步
  │
[空闲] (等待服务器响应)
  ↓
[已连接] (onOpen 触发)
  ├─→ 发送初始元数据
  ├─→ 触发 mod_audio_stream::connect 事件
  ↓
[流传输中] (capture_callback 读取帧)
  ├─→ stream_frame() 在每个音频帧上运行
  ├─→ 帧聚合并作为二进制发送
  ├─→ 处理服务器响应，触发事件
  ├─→ 可以暂停 (audio_paused=1)
  ├─→ 可以恢复 (audio_paused=0)
  │
  暂停/恢复:
    stream_session_pauseresume(pause_flag)
      → tech_pvt->audio_paused = flag
      → stream_frame() 检查标志，如果暂停则跳过
  │
  错误:
    连接错误 → eventCallback(CONNECT_ERROR)
      → 触发 mod_audio_stream::error 事件
      → media_bug_close() 触发（关闭音频流）
  │
  断开连接:
    连接断开 → eventCallback(CONNECTION_DROPPED)
      → 触发 mod_audio_stream::disconnect 事件
  │
[停止中] (调用 stop API 或通道关闭)
  ├─→ close_requested = 1
  ├─→ stream_session_cleanup(text, channelIsClosing)
  │     ├─→ 设置 cleanup_started 标志
  │     ├─→ 发送最终文本消息（如果提供）
  │     ├─→ deleteFiles() - 删除临时音频文件
  │     ├─→ streamer->disconnect()
  │     ├─→ destroy_tech_pvt()
  │     └─→ 移除媒体钩子（或 FS 处理，如果通道关闭）
  ↓
[已清理]
  └─→ 会话终止
```

---

## 音频编码格式

### 流传输到服务器

**格式**: L16（16位有符号 PCM，线性编码）
- **字节序**: 大端序（网络字节序）
- **采样大小**: 16 位（2 字节）
- **编码**: 线性 PCM（无压缩）
- **声道**: 1（单声道）或 2（立体声）
- **采样率**: 可配置（8000 Hz, 16000 Hz 等）

**示例帧**:
- 采样率: 8000 Hz
- 声道: 1（单声道）
- 持续时间: 20ms
- 采样点数: 8000 * 0.02 = 160
- 数据大小: 160 * 2 = 320 字节

### 从服务器接收

支持多种音频格式（通过 `audioDataType` 字段）：

1. **raw** - 原始 PCM 数据
   - 文件扩展名: `.r8`
   - 编码: 线性 PCM
   - 需要指定 `sampleRate`

2. **wav** - WAV 容器格式
   - 文件扩展名: `.wav`
   - 包含头信息的完整 WAV 文件

3. **mp3** - MPEG 音频层 3
   - 文件扩展名: `.mp3`
   - 压缩音频格式

4. **ogg** - OGG 容器
   - 文件扩展名: `.ogg`
   - 通常包含 Vorbis 编码

5. **pcmu** / **pcma** - μ-law / A-law
   - 文件扩展名: `.ulaw` / `.alaw`
   - 电信标准编码

---

## 依赖库说明

### 核心依赖

1. **libfreeswitch-dev**
   - FreeSWITCH 开发库
   - 提供核心 API 和数据结构

2. **libssl-dev**
   - OpenSSL 开发库
   - 用于 WSS（WebSocket Secure）连接
   - TLS/SSL 加密

3. **zlib1g-dev**
   - zlib 压缩库
   - 用于 per-message deflate 压缩

4. **libevent-dev**
   - 事件通知库
   - libwsc 的基础，提供异步 I/O

5. **libspeexdsp-dev**
   - Speex DSP 库
   - 提供高质量音频重采样

### 子模块

**libwsc** - WebSocket 客户端库
- 位置: `libs/libwsc`
- 特性:
  - RFC-6455 标准兼容
  - 基于 libevent，轻量级
  - 优化低延迟音频流
  - 支持 WSS（TLS）
  - Per-message deflate 压缩
  - 自定义头支持

---

## 构建和安装

### 构建步骤

1. **克隆仓库并初始化子模块**
   ```bash
   git clone https://github.com/amigniter/mod_audio_stream.git
   cd mod_audio_stream
   git submodule init
   git submodule update
   ```

2. **设置 PKG_CONFIG_PATH（如果从源码构建 FreeSWITCH）**
   ```bash
   export PKG_CONFIG_PATH=/usr/local/freeswitch/lib/pkgconfig
   ```

3. **构建模块**
   ```bash
   mkdir build && cd build
   cmake -DCMAKE_BUILD_TYPE=Release ..
   make
   sudo make install
   ```

4. **启用 TLS 支持（可选）**
   ```bash
   cmake -DCMAKE_BUILD_TYPE=Release -DUSE_TLS=ON ..
   ```

### DEB 包构建

```bash
cd build
cpack -G DEB
```

Debian 包将放置在 `_packages` 文件夹中。

### 自动化安装脚本

```bash
sudo apt-get -y install git \
    && cd /usr/src/ \
    && git clone https://github.com/amigniter/mod_audio_stream.git \
    && cd mod_audio_stream \
    && sudo bash ./build-mod-audio-stream.sh
```

---

## 实际应用场景

### 1. 实时语音转文本（ASR）

**场景**: 将呼叫音频实时流式传输到云端 ASR 服务进行转录。

**实现**:
```xml
<!-- 拨号计划配置 -->
<extension name="asr_streaming">
  <condition field="destination_number" expression="^9999$">
    <action application="answer"/>
    <action application="set" data="STREAM_BUFFER_SIZE=100"/>
    <action application="set" data="STREAM_EXTRA_HEADERS={'Authorization':'Bearer YOUR_API_KEY'}"/>
    <action application="uuid_audio_stream" data="${uuid} start wss://asr.example.com/stream mono 16k {'language':'zh-CN'}"/>
    <action application="playback" data="silence_stream://30000"/>
  </condition>
</extension>
```

**事件处理**（ESL 脚本）:
```python
import ESL

con = ESL.ESLconnection("localhost", "8021", "ClueCon")
con.events("plain", "CUSTOM mod_audio_stream::json")

while True:
    e = con.recvEvent()
    if e:
        name = e.getHeader("Event-Subclass")
        if name == "mod_audio_stream::json":
            body = e.getBody()
            # 处理转录结果
            print(f"转录: {body}")
```

### 2. 双向对话机器人

**场景**: 与 AI 对话系统进行实时交互，系统可以发送语音响应。

**实现**:
```javascript
// WebSocket 服务器端（Node.js 示例）
const WebSocket = require('ws');

const wss = new WebSocket.Server({ port: 8080 });

wss.on('connection', (ws) => {
    ws.on('message', (message) => {
        if (typeof message === 'string') {
            // 文本消息（元数据或命令）
            const data = JSON.parse(message);
            console.log('收到元数据:', data);
        } else {
            // 二进制音频数据（L16 PCM）
            processAudio(message);
        }
    });
    
    // 发送音频响应
    function sendAudioResponse(audioBuffer) {
        const response = {
            type: 'streamAudio',
            data: {
                audioDataType: 'raw',
                sampleRate: 8000,
                audioData: audioBuffer.toString('base64')
            }
        };
        ws.send(JSON.stringify(response));
    }
});
```

### 3. 呼叫质量监控

**场景**: 实时分析呼叫音频质量，检测静音、噪音或其他异常。

**实现**: 流式传输音频到分析服务，接收质量指标事件，根据需要调整呼叫参数。

### 4. 实时翻译服务

**场景**: 实时翻译呼叫者的语音到另一种语言。

**流程**:
1. 流式传输呼叫者音频到翻译服务
2. 服务返回翻译后的音频（通过 streamAudio）
3. 播放翻译音频给被叫者

---

## 调试技巧

### 1. 启用详细日志

```bash
# FreeSWITCH 控制台
freeswitch@localhost> console loglevel debug
```

### 2. 监听模块事件

```bash
# ESL 事件监听
freeswitch@localhost> events plain CUSTOM mod_audio_stream::connect
freeswitch@localhost> events plain CUSTOM mod_audio_stream::json
freeswitch@localhost> events plain CUSTOM mod_audio_stream::error
```

### 3. 检查 WebSocket 连接

```bash
# 使用 tcpdump 捕获 WebSocket 流量
sudo tcpdump -i any -w websocket.pcap port 8080

# 使用 Wireshark 分析
wireshark websocket.pcap
```

### 4. 测试 WebSocket 服务器

```javascript
// 简单的 WebSocket 回显服务器（Node.js）
const WebSocket = require('ws');
const wss = new WebSocket.Server({ port: 8080 });

wss.on('connection', (ws) => {
    console.log('客户端已连接');
    
    ws.on('message', (message) => {
        console.log(`收到 ${message.length} 字节`);
        // 回显文本消息
        if (typeof message === 'string') {
            ws.send(message);
        }
    });
    
    ws.on('close', () => {
        console.log('客户端已断开');
    });
});

console.log('WebSocket 服务器运行在 ws://localhost:8080');
```

### 5. 分析音频数据

```python
# 将接收的二进制数据转换为可播放音频
import struct
import wave

def binary_to_wav(binary_data, output_file, sample_rate=8000, channels=1):
    """将 L16 PCM 数据转换为 WAV 文件"""
    samples = struct.unpack('>' + 'h' * (len(binary_data) // 2), binary_data)
    
    with wave.open(output_file, 'w') as wav:
        wav.setnchannels(channels)
        wav.setsampwidth(2)  # 16 位 = 2 字节
        wav.setframerate(sample_rate)
        wav.writeframes(binary_data)

# 使用示例
with open('received_audio.bin', 'rb') as f:
    audio_data = f.read()
    binary_to_wav(audio_data, 'output.wav', sample_rate=8000, channels=1)
```

---

## 性能优化建议

### 1. 缓冲区大小调整

```xml
<!-- 较大的缓冲区减少网络开销，但增加延迟 -->
<action application="set" data="STREAM_BUFFER_SIZE=100"/>  <!-- 100ms 包 -->

<!-- 较小的缓冲区降低延迟，但增加网络开销 -->
<action application="set" data="STREAM_BUFFER_SIZE=20"/>   <!-- 20ms 包（默认） -->
```

**建议**:
- ASR 应用: 60-100ms（平衡延迟和带宽）
- 低延迟对话: 20-40ms
- 批处理分析: 100-200ms

### 2. 压缩配置

```xml
<!-- 禁用压缩以降低 CPU 使用（如果带宽充足） -->
<action application="set" data="STREAM_MESSAGE_DEFLATE=1"/>

<!-- 启用压缩以节省带宽（默认） -->
<action application="unset" data="STREAM_MESSAGE_DEFLATE"/>
```

### 3. 心跳间隔

```xml
<!-- 30 秒心跳防止空闲连接超时 -->
<action application="set" data="STREAM_HEART_BEAT=30"/>
```

### 4. 采样率选择

```xml
<!-- 8kHz - 最低带宽，适合电话质量语音 -->
<action application="uuid_audio_stream" data="${uuid} start ws://... mono 8k"/>

<!-- 16kHz - 更高质量，适合现代 ASR -->
<action application="uuid_audio_stream" data="${uuid} start ws://... mono 16k"/>
```

**带宽计算**:
- 8kHz mono: ~128 kbps（16位 PCM）
- 16kHz mono: ~256 kbps
- 16kHz stereo: ~512 kbps

---

## 常见问题排查

### 问题 1: WebSocket 连接失败

**症状**: `mod_audio_stream::error` 事件，代码 6（CONNECT_FAILED）

**可能原因**:
- WebSocket URL 错误
- 网络连接问题
- 防火墙阻止
- DNS 解析失败

**解决方案**:
```bash
# 测试网络连接
ping ws-server.example.com

# 测试端口
telnet ws-server.example.com 8080

# 检查 DNS
nslookup ws-server.example.com
```

### 问题 2: TLS 握手失败

**症状**: `mod_audio_stream::error` 事件，代码 8（SSL_HANDSHAKE_FAILED）

**可能原因**:
- 证书无效或过期
- CA 证书不正确
- 主机名不匹配

**解决方案**:
```xml
<!-- 禁用主机名验证（仅用于测试） -->
<action application="set" data="STREAM_TLS_DISABLE_HOSTNAME_VALIDATION=1"/>

<!-- 指定自定义 CA 证书 -->
<action application="set" data="STREAM_TLS_CA_FILE=/path/to/ca-bundle.crt"/>

<!-- 禁用证书验证（不推荐用于生产） -->
<action application="set" data="STREAM_TLS_CA_FILE=NONE"/>
```

### 问题 3: 音频数据不完整或损坏

**症状**: 服务器接收到的音频有间隙或损坏

**可能原因**:
- 网络丢包
- 缓冲区配置不当
- CPU 过载

**解决方案**:
```xml
<!-- 增加缓冲区大小 -->
<action application="set" data="STREAM_BUFFER_SIZE=60"/>

<!-- 检查系统资源 -->
```

```bash
# 监控 CPU 使用
top -p $(pidof freeswitch)

# 检查网络统计
netstat -s | grep -i error
```

### 问题 4: 内存泄漏

**症状**: FreeSWITCH 内存使用持续增长

**诊断**:
```bash
# 监控内存
watch -n 1 'ps aux | grep freeswitch'

# 使用 Valgrind（开发环境）
valgrind --leak-check=full freeswitch -nonat
```

**可能原因**:
- 临时文件未清理
- WebSocket 连接未正确关闭
- 会话清理不当

**解决方案**: 确保始终调用 `stop` 命令或正确关闭通道

---

## 总结

**mod_audio_stream** 是一个设计精良、生产就绪的 FreeSWITCH 模块，提供以下核心价值：

### 技术优势

1. **生产级质量**
   - 正确的生命周期管理
   - 线程安全设计
   - 可预测的内存使用
   - 高并发支持（5000+ 通话）

2. **灵活性**
   - 多种音频混合模式（mono/mixed/stereo）
   - 可配置采样率
   - 可调整缓冲区大小
   - 自定义头支持

3. **双向通信**
   - 流式传输音频到服务器
   - 从服务器接收音频播放
   - 实时文本消息交换

4. **安全性**
   - WSS（WebSocket Secure）支持
   - TLS 1.2+ 加密
   - 证书验证
   - 客户端证书支持

### 应用价值

- **实时 ASR**: 语音转文本
- **对话 AI**: 智能语音助手
- **质量监控**: 通话质量分析
- **实时翻译**: 跨语言通信
- **录音分析**: 后续处理和分析

### 代码质量

- **清晰的架构**: 分层设计，职责明确
- **良好的错误处理**: 详细的错误代码和消息
- **完整的文档**: API、事件、配置完整记录
- **活跃维护**: 持续更新和改进

这个项目展示了如何在复杂的实时系统（FreeSWITCH）中正确实现 WebSocket 音频流，同时保持代码的可维护性和生产可靠性。
